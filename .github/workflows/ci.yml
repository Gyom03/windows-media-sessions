name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Test
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies (workspace root)
        run: npm install --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm --workspace packages/node run typecheck

      - name: Test
        run: npm test

      - name: Build .NET backend
        shell: pwsh
        run: ./scripts/build-backend.ps1

      - name: Build TypeScript package
        run: npm --workspace packages/node run build

      - name: Verify package contents
        working-directory: packages/node
        shell: pwsh
        run: |
          npm pack --dry-run
          if (-not (Test-Path 'bin/win-x64/windows-media-sessions-backend.exe')) {
            throw 'Backend executable missing from bin/'
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-media-sessions-pkg
          path: packages/node/
          if-no-files-found: error
